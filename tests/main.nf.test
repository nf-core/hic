nextflow_pipeline {

    name "Test pipeline"
    script "../main.nf"
    tag "PIPELINE"
    config "conf/test.config"

    test("Run with profile test") {

        when {
            params {
                outdir = "$outputDir"
            }
        }

        then {
            assert workflow.success

            // TODO
            // assertAll(
            // )
        }
    }

    test("Should split fastqs") {

        when {
            params {
                outdir = "$outputDir"
                split_fastq = true
            }
        }

        then {
            assert workflow.success

            // TODO
            // assertAll(
            // )
        }
    }
}
